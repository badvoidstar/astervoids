{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "location": {
      "value": "${AZURE_LOCATION}"
    },
    "customDomainName": {
      "value": "${CUSTOM_DOMAIN_NAME=}"
    },
    "customSubdomain": {
      "value": "${CUSTOM_SUBDOMAIN=}"
    },
    "useSharedInfra": {
      "value": "${USE_SHARED_INFRA=false}"
    },
    "certKeyVaultSecretUrl": {
      "value": "${CERT_KEY_VAULT_SECRET_URL=}"
    },
    "certKeyVaultCertName": {
      "value": "${CERT_KEY_VAULT_CERT_NAME=}"
    },
    "certReaderIdentityId": {
      "value": "${CERT_READER_IDENTITY_ID=}"
    }
  }
}
