name: Azure CICD

on:
  push:
    branches:
      - '**'  # All branches
    paths-ignore:
      - '**.md'
      - '.gitignore'
  pull_request:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '.gitignore'
  workflow_dispatch:  # Allow manual trigger

permissions:
  id-token: write  # Required for OIDC authentication
  contents: read

env:
  DOTNET_VERSION: '10.0.x'
  # Shared resource naming conventions
  PRODUCTION_RG: 'rg-production'
  CONTAINER_ENV_NAME: 'cae-production'
  CONTAINER_APP_PREFIX: 'ca-web'
  # ── Multi-region rollout ──────────────────────────────────────────────────
  # Source of truth: the GitHub repository variable REGIONS_JSON
  # (Settings → Secrets and variables → Actions → Variables tab).
  # Empty string preserves the legacy single-region production deploy
  # (everything in westus2 under cae-production). To roll out multi-region,
  # set the REGIONS_JSON repo variable to a JSON array of region objects:
  #
  #   [
  #     {"name":"westus2","location":"westus2","displayName":"US West"},
  #     {"name":"northeurope","location":"northeurope","displayName":"Europe"},
  #     {"name":"japaneast","location":"japaneast","displayName":"Asia"}
  #   ]
  #
  # The first entry is the "primary" region — it owns the shared ACR and
  # branch deployments target its CAE. Only applied when deploying main;
  # branches always go single-region into the primary region's CAE.
  REGIONS_JSON: ${{ vars.REGIONS_JSON }}
  # ── BYO TLS certificate (optional) ────────────────────────────────────────
  # When all three are set, bicep binds <subdomain>.<domain> on every region
  # (and every branch deploy) to a Key Vault-stored certificate — no per-app
  # DigiCert managed-cert provisioning runs. Recommended path for multi-region
  # where per-region ACA hostnames are bound with one shared wildcard cert.
  # See ARCHITECTURE.md "BYO wildcard cert for regional hostnames".
  #
  # When ANY is empty, falls back to the legacy DigiCert managed-cert flow
  # (works in single-region only; multi-region apex TLS will fail).
  #
  # Set via repo variables (Settings → Secrets and variables → Actions → Variables):
  #   CERT_KEY_VAULT_SECRET_URL    e.g. https://my-kv.vault.azure.net/secrets/wildcard-your-domain-com
  #   CERT_KEY_VAULT_CERT_NAME     e.g. wildcard-your-domain-com (stable name for the CAE cert resource)
  #   CERT_READER_IDENTITY_ID      resource ID of a user-assigned managed identity that has
  #                                 'Key Vault Certificate User' role on the KV holding the cert.
  #                                 OPTIONAL in production when bicep's manageAcmebotPermissions=true
  #                                 (the default — bicep creates id-acme-cert-reader and uses its
  #                                 output for the CAE cert binding). STILL REQUIRED for branch deploys
  #                                 (the bootstrap step needs the ID to attach the identity to the
  #                                 shared CAE before creating the cert resource).
  CERT_KEY_VAULT_SECRET_URL: ${{ vars.CERT_KEY_VAULT_SECRET_URL }}
  CERT_KEY_VAULT_CERT_NAME: ${{ vars.CERT_KEY_VAULT_CERT_NAME }}
  CERT_READER_IDENTITY_ID: ${{ vars.CERT_READER_IDENTITY_ID }}

jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore astervoids.sln

      - name: Build solution
        run: dotnet build astervoids.sln --configuration Release --no-restore

      - name: Run tests
        run: dotnet test astervoids.sln --configuration Release --no-build --verbosity normal

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Run JavaScript tests
        run: node --test AstervoidsWeb/*.test.mjs

      - name: Validate workflow helpers
        run: bash .github/scripts/workflow-helpers.test.sh

      # Bicep smoke check — fail fast on template syntax errors before any
      # Azure deploy is attempted. Runs in the build job so a malformed
      # bicep change blocks the deploy job from even running.
      - name: Bicep build (template smoke check)
        run: |
          # az is preinstalled on ubuntu-latest runners; bicep CLI auto-installs on first use.
          az bicep build --file infra/main.bicep --outfile /tmp/main.compiled.json
          test -s /tmp/main.compiled.json
          echo "✅ infra/main.bicep compiles"

  deploy:
    name: Deploy to Azure
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set deployment variables
        id: vars
        run: |
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT
          
          # Sanitize branch name using shared script
          chmod +x .github/scripts/sanitize-branch-name.sh
          SANITIZED=$(.github/scripts/sanitize-branch-name.sh "$BRANCH_NAME")
          echo "sanitized_branch=$SANITIZED" >> $GITHUB_OUTPUT
          
          # Determine if this is production (main branch)
          if [ "$BRANCH_NAME" = "main" ]; then
            echo "is_production=true" >> $GITHUB_OUTPUT
            echo "env_name=production" >> $GITHUB_OUTPUT
          else
            echo "is_production=false" >> $GITHUB_OUTPUT
            echo "env_name=$SANITIZED" >> $GITHUB_OUTPUT
          fi

      - name: Install Azure Developer CLI
        run: |
          curl -fsSL https://aka.ms/install-azd.sh | bash

      - name: Log in to Azure using OIDC
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Register required Azure providers
        run: |
          # Register Microsoft.App provider (required for Container Apps)
          az provider register -n Microsoft.App --wait || true
          az provider register -n Microsoft.OperationalInsights --wait || true

      - name: Authenticate azd with federated credentials
        run: |
          azd auth login --client-id "${{ secrets.AZURE_CLIENT_ID }}" \
                         --federated-credential-provider github \
                         --tenant-id "${{ secrets.AZURE_TENANT_ID }}"

      - name: Check production infrastructure (for branch deployments)
        if: steps.vars.outputs.is_production != 'true'
        run: |
          if ! az group show --name "${{ env.PRODUCTION_RG }}" &>/dev/null; then
            echo "::error::Production environment must be deployed first. Deploy main branch before deploying feature branches."
            exit 1
          fi

      - name: Set up azd environment
        run: |
          source .github/scripts/deployment-helpers.sh
          ENV_NAME="${{ steps.vars.outputs.env_name }}"
          
          # Create or select azd environment
          azd env new "$ENV_NAME" --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }} --location westus2 || true
          azd env select "$ENV_NAME"
          
          # Set location
          azd env set AZURE_LOCATION westus2
          # Pass through region matrix so non-production shared-infra deploys
          # can resolve the primary shared CAE deterministically.
          azd env set REGIONS_JSON "${REGIONS_JSON:-[]}"
          
          # For branch deployments, set resource group to production's resource group
          # This tells azd deploy where to find the container app
          if [ "${{ steps.vars.outputs.is_production }}" != "true" ]; then
            azd env set AZURE_RESOURCE_GROUP "${{ env.PRODUCTION_RG }}"
            azd env set USE_SHARED_INFRA "true"
            # Tell azd which container app to deploy to (avoids tag collision with production)
            azd env set SERVICE_WEB_RESOURCE_NAME "${{ env.CONTAINER_APP_PREFIX }}-${{ steps.vars.outputs.sanitized_branch }}"
          fi
          
          # Set custom domain from secrets (if provided)
          if [ -n "${{ secrets.CUSTOM_DOMAIN_NAME }}" ]; then
            azd env set CUSTOM_DOMAIN_NAME "${{ secrets.CUSTOM_DOMAIN_NAME }}"
          fi
          
          # Set subdomain (with branch suffix for non-production)
          if [ -n "${{ secrets.CUSTOM_SUBDOMAIN }}" ]; then
            if [ "${{ steps.vars.outputs.is_production }}" = "true" ]; then
              azd env set CUSTOM_SUBDOMAIN "${{ secrets.CUSTOM_SUBDOMAIN }}"
            else
              azd env set CUSTOM_SUBDOMAIN "${{ secrets.CUSTOM_SUBDOMAIN }}-${{ steps.vars.outputs.sanitized_branch }}"
            fi
          fi

          # Seed DOMAIN_VERIFICATION_ID so bicep can emit asuid TXT before
          # branch custom hostname binding is attempted. Azure validates
          # ownership during app create; missing asuid causes
          # InvalidCustomHostNameValidation failures.
          if [ -n "${{ secrets.CUSTOM_DOMAIN_NAME }}" ]; then
            PRIMARY_REGION_NAME=$(echo "$REGIONS_JSON" | jq -r '.[0].name' 2>/dev/null || true)
            SHARED_CAE_NAME="${{ env.CONTAINER_ENV_NAME }}"
            if [ -n "$PRIMARY_REGION_NAME" ]; then
              SHARED_CAE_NAME="${{ env.CONTAINER_ENV_NAME }}-${PRIMARY_REGION_NAME}"
            fi
            DOMAIN_VERIFICATION_ID=$(resolve_domain_verification_id \
              "${{ env.PRODUCTION_RG }}" \
              "ca-web-production-${PRIMARY_REGION_NAME}" \
              "ca-web-production" \
              "${{ env.CONTAINER_APP_PREFIX }}-${{ steps.vars.outputs.sanitized_branch }}" \
              -- "$SHARED_CAE_NAME")
            if [ -n "$DOMAIN_VERIFICATION_ID" ]; then
              azd env set DOMAIN_VERIFICATION_ID "$DOMAIN_VERIFICATION_ID"
            else
              echo "::warning::Could not resolve customDomainVerificationId from existing container apps."
            fi

            # BYO + branch custom hostname needs the asuid token up-front.
            if [ "${{ steps.vars.outputs.is_production }}" != "true" ] \
              && [ -n "$CERT_KEY_VAULT_SECRET_URL" ] \
              && [ -n "$CERT_KEY_VAULT_CERT_NAME" ] \
              && [ -n "$CERT_READER_IDENTITY_ID" ] \
              && [ -z "$DOMAIN_VERIFICATION_ID" ]; then
              echo "::error::Branch BYO custom-domain deploy requires DOMAIN_VERIFICATION_ID, but none was found."
              echo "::error::Resolve by ensuring at least one container app exists in ${{ env.PRODUCTION_RG }} and exposes properties.customDomainVerificationId."
              exit 1
            fi
          fi

          # BYO TLS cert (optional) — passed through to bicep when all three
          # vars are set. azd reads these via main.parameters.json env-var
          # substitution (see infra/main.parameters.json).
          if [ -n "$CERT_KEY_VAULT_SECRET_URL" ]; then
            azd env set CERT_KEY_VAULT_SECRET_URL "$CERT_KEY_VAULT_SECRET_URL"
            azd env set CERT_KEY_VAULT_CERT_NAME "$CERT_KEY_VAULT_CERT_NAME"
            azd env set CERT_READER_IDENTITY_ID "$CERT_READER_IDENTITY_ID"
          fi

      - name: Bootstrap BYO cert on shared CAE (branches only)
        # Production deploys handle BYO cert + identity attachment via bicep
        # (container-apps.bicep adds properties.identity.userAssignedIdentities
        # on the CAE + a managedEnvironments/certificates resource referencing
        # it). Branch deploys reference an EXISTING CAE via 'existing' and
        # can't safely modify its properties through bicep without risking
        # collisions, so we do the equivalent setup here via az CLI before
        # bicep runs.
        #
        # Both operations are idempotent:
        #   - `az containerapp env identity assign` is a no-op if already attached.
        #   - The cert-existence check skips creation when the named cert is present.
        # So this step is safe to run on every branch push regardless of whether
        # production has already deployed with BYO (it has, → no-op; it hasn't, →
        # creates both). After this step succeeds, bicep's webBranch module can
        # bind the branch hostname to the cert with bindingType=SniEnabled.
        #
        # The custom-domain check is intentionally omitted: env.CUSTOM_DOMAIN_NAME
        # would be empty (it's a secret, not an env var), and bootstrapping the
        # cert when no domain is configured is just a few wasted az calls — the
        # cert sits unbound until something references it.
        if: steps.vars.outputs.is_production != 'true' && env.CERT_KEY_VAULT_SECRET_URL != '' && env.CERT_KEY_VAULT_CERT_NAME != '' && env.CERT_READER_IDENTITY_ID != ''
        run: |
          # Single-region branches target 'cae-production'. After multi-region
          # production is rolled out (REGIONS_JSON populated), branches target
          # the primary shared CAE (cae-production-<primary-region>).
          PRIMARY_REGION_NAME=$(echo "$REGIONS_JSON" | jq -r '.[0].name' 2>/dev/null || true)
          CAE_NAME="${{ env.CONTAINER_ENV_NAME }}"
          if [ -n "$PRIMARY_REGION_NAME" ] && [ "$PRIMARY_REGION_NAME" != "null" ]; then
            CAE_NAME="${{ env.CONTAINER_ENV_NAME }}-${PRIMARY_REGION_NAME}"
          fi
          RG="${{ env.PRODUCTION_RG }}"

          echo "::group::Bootstrap BYO cert on $CAE_NAME"

          # Defensive check: the shared CAE must already exist (production
          # owns its creation). If it doesn't, fail clearly — the branch
          # deploy would fail later in confusing ways otherwise.
          if ! az containerapp env show --resource-group "$RG" --name "$CAE_NAME" &>/dev/null; then
            echo "::error::Shared CAE '$CAE_NAME' not found in '$RG'."
            echo "::error::Production must be deployed first to create the CAE."
            echo "::endgroup::"
            exit 1
          fi

          # 1. Attach the user-assigned identity if not already attached.
          # `identity assign` is a PATCH that adds the identity to the
          # userAssignedIdentities collection; no-op if already present.
          echo "Attaching identity $CERT_READER_IDENTITY_ID to CAE $CAE_NAME (idempotent)..."
          az containerapp env identity assign \
            --resource-group "$RG" \
            --name "$CAE_NAME" \
            --user-assigned "$CERT_READER_IDENTITY_ID" \
            --output none

          # 2. Create the cert resource on the CAE if it doesn't already exist.
          # The az CLI's `az containerapp env certificate create` only supports
          # the managed-cert path (--hostname + --validation-method). For
          # KV-backed BYO certs we PUT the ARM resource directly — same shape
          # as bicep's Microsoft.App/managedEnvironments/certificates resource.
          CERT_EXISTS=$(az containerapp env certificate list \
            --resource-group "$RG" --name "$CAE_NAME" \
            --query "[?name=='$CERT_KEY_VAULT_CERT_NAME'].name" -o tsv 2>/dev/null || echo "")
          if [ -z "$CERT_EXISTS" ]; then
            echo "Cert '$CERT_KEY_VAULT_CERT_NAME' not present on CAE — PUT'ing ARM resource from Key Vault..."
            CAE_LOCATION=$(az containerapp env show \
              --resource-group "$RG" --name "$CAE_NAME" \
              --query location -o tsv)
            CERT_ARM_URL="https://management.azure.com/subscriptions/${{ secrets.AZURE_SUBSCRIPTION_ID }}/resourceGroups/${RG}/providers/Microsoft.App/managedEnvironments/${CAE_NAME}/certificates/${CERT_KEY_VAULT_CERT_NAME}?api-version=2024-10-02-preview"
            BODY=$(jq -n \
              --arg loc "$CAE_LOCATION" \
              --arg identity "$CERT_READER_IDENTITY_ID" \
              --arg kvurl "$CERT_KEY_VAULT_SECRET_URL" \
              '{
                location: $loc,
                properties: {
                  certificateKeyVaultProperties: {
                    identity: $identity,
                    keyVaultUrl: $kvurl
                  }
                }
              }')
            TMP_BODY=$(mktemp)
            echo "$BODY" > "$TMP_BODY"
            az rest --method PUT --uri "$CERT_ARM_URL" --body "@$TMP_BODY" --output none
            rm -f "$TMP_BODY"
            echo "✅ Cert created via ARM PUT"
          else
            echo "✅ Cert '$CERT_KEY_VAULT_CERT_NAME' already present on CAE — no action needed"
          fi
          echo "::endgroup::"

      - name: Provision and Deploy to Azure
        id: deploy
        run: |
          source .github/scripts/deployment-helpers.sh
          IS_PRODUCTION="${{ steps.vars.outputs.is_production }}"
          SANITIZED_BRANCH="${{ steps.vars.outputs.sanitized_branch }}"
          # ── Multi-region detection ────────────────────────────────────────
          # Only the main branch (production) can be multi-region. Branches
          # always deploy single-region into the primary region's CAE.
          # REGIONS_JSON is parsed once here so the script can branch on
          # `IS_MULTI_REGION` without re-parsing.
          IS_MULTI_REGION="false"
          if [ "$IS_PRODUCTION" = "true" ] && [ -n "$REGIONS_JSON" ]; then
            # Validate it's a non-empty JSON array; fail loudly if it isn't.
            REGION_COUNT=$(echo "$REGIONS_JSON" | jq 'length' 2>/dev/null || echo "0")
            if [ "$REGION_COUNT" -gt 0 ] 2>/dev/null; then
              IS_MULTI_REGION="true"
              echo "::notice::Multi-region production deploy: $REGION_COUNT regions"
            else
              echo "::warning::REGIONS_JSON is set but did not parse as a non-empty array. Falling back to single-region."
            fi
          fi

          if [ "$IS_PRODUCTION" = "true" ] && [ "$IS_MULTI_REGION" = "false" ]; then
            # Production single-region (legacy): use azd up for full provision + deploy
            azd up --no-prompt
          elif [ "$IS_MULTI_REGION" = "true" ]; then
            # ── Production multi-region ───────────────────────────────────
            # We bypass `azd up` because azd doesn't know about per-region
            # container apps + static-apex publish. Instead:
            #   1. Run bicep directly with regions=<JSON>, which provisions
            #      per-region CAEs/apps + Static Web App apex.
            #   2. Build the Docker image once.
            #   3. Push it to the shared ACR.
            #   4. Update each region's container app to point at the new image.
            #   5. Publish static apex assets + region bootstrap manifest.
            # Each region's container app already has Region__Id env var
            # baked in by the bicep template; only the image tag changes
            # on subsequent deploys.
            AZD_ENV_VALUES=$(azd env get-values)
            CD_NAME=$(azd_env_value CUSTOM_DOMAIN_NAME "$AZD_ENV_VALUES")
            CD_SUB=$(azd_env_value CUSTOM_SUBDOMAIN "$AZD_ENV_VALUES")
            PRIMARY_LOCATION=$(echo "$REGIONS_JSON" | jq -r '.[0].location')

            # ── Fail-fast: multi-region + custom domain REQUIRES BYO cert ──
            # Even with static apex hosting, gameplay traffic still targets
            # per-region ACA custom hostnames (<subdomain>-<region>.<domain>).
            # We bind those with a shared wildcard cert from Key Vault.
            if [ -n "$CD_NAME" ] && [ -z "$CERT_KEY_VAULT_SECRET_URL" ]; then
              echo "::error::Multi-region with a custom domain requires BYO cert vars."
              echo "::error::  CUSTOM_DOMAIN_NAME is set ('$CD_NAME') but CERT_KEY_VAULT_SECRET_URL is empty."
              echo "::error::  Regional ACA hostnames require wildcard BYO cert wiring."
              echo "::error::  Set vars.CERT_KEY_VAULT_SECRET_URL, vars.CERT_KEY_VAULT_CERT_NAME, and"
              echo "::error::  vars.CERT_READER_IDENTITY_ID. See ARCHITECTURE.md -> 'BYO wildcard cert for regional hostnames'."
              exit 1
            fi

            # Per-subscription customDomainVerificationId. Read from any
            # existing container app — it's the same value across every
            # app in the subscription. We try the primary-region app first
            # (post-rollout state), then the legacy single-region app
            # (pre-rollout state). Empty result means greenfield with no
            # existing apps; bicep will skip per-region asuid TXT records
            # and CNAME-only validation is used (which may need a 2nd
            # bicep pass for hostname binding to succeed on first deploy).
            PRIMARY_REGION_NAME=$(echo "$REGIONS_JSON" | jq -r '.[0].name')
            DOMAIN_VERIFICATION_ID=$(resolve_domain_verification_id \
              "${{ env.PRODUCTION_RG }}" \
              "ca-web-production-${PRIMARY_REGION_NAME}" \
              "ca-web-production" \
              -- \
              "cae-production-${PRIMARY_REGION_NAME}" \
              "cae-production")
            if [ -z "$DOMAIN_VERIFICATION_ID" ]; then
              echo "::warning::No existing app or CAE found to extract customDomainVerificationId. asuid TXT records will be skipped (CNAME-only). A second deploy may be needed to bind hostnames."
            fi

            echo "Provisioning multi-region production via bicep (primary: $PRIMARY_LOCATION)"
            set +e
            deploy_multi_region_bicep \
              "production-multi-${{ github.run_id }}" \
              "$PRIMARY_LOCATION" \
              "$CD_NAME" \
              "$CD_SUB" \
              "$REGIONS_JSON" \
              "$CERT_KEY_VAULT_SECRET_URL" \
              "$CERT_KEY_VAULT_CERT_NAME" \
              "$CERT_READER_IDENTITY_ID" \
              "$DOMAIN_VERIFICATION_ID"
            DEPLOY_RC=$?
            set -e
            if [ "$DEPLOY_RC" -ne 0 ]; then
              if [ -n "$CD_NAME" ] && [ -z "$DOMAIN_VERIFICATION_ID" ]; then
                echo "::warning::First multi-region deploy pass failed without domainVerificationId. Retrying once after reading it from the new shared CAE."
                DOMAIN_VERIFICATION_ID=$(resolve_domain_verification_id \
                  "${{ env.PRODUCTION_RG }}" \
                  -- "cae-production-${PRIMARY_REGION_NAME}")
                if [ -z "$DOMAIN_VERIFICATION_ID" ]; then
                  echo "::error::Retry aborted: could not resolve customDomainVerificationId from cae-production-${PRIMARY_REGION_NAME}."
                  exit "$DEPLOY_RC"
                fi
                deploy_multi_region_bicep \
                  "production-multi-${{ github.run_id }}" \
                  "$PRIMARY_LOCATION" \
                  "$CD_NAME" \
                  "$CD_SUB" \
                  "$REGIONS_JSON" \
                  "$CERT_KEY_VAULT_SECRET_URL" \
                  "$CERT_KEY_VAULT_CERT_NAME" \
                  "$CERT_READER_IDENTITY_ID" \
                  "$DOMAIN_VERIFICATION_ID"
              else
                exit "$DEPLOY_RC"
              fi
            fi

            # Fetch ALL outputs once, then look them up case-insensitively
            # via jq. Azure ARM serializes output names through .NET's
            # JsonNamingPolicy.CamelCase which lowercases all but the LAST
            # char of leading acronyms (so `AZURE_CONTAINER_REGISTRY_NAME`
            # comes back as `azurE_CONTAINER_REGISTRY_NAME`). JMESPath is
            # case-sensitive, so the obvious `--query` calls return empty;
            # using jq's `ascii_downcase` avoids hardcoding the mangled
            # spelling and keeps the workflow robust to future ARM changes.
            OUTPUTS_JSON=$(az deployment sub show \
              --name "production-multi-${{ github.run_id }}" \
              --query 'properties.outputs' -o json)
            ACR_NAME=$(deployment_output "$OUTPUTS_JSON" AZURE_CONTAINER_REGISTRY_NAME)
            ACR_LOGIN_SERVER=$(deployment_output "$OUTPUTS_JSON" AZURE_CONTAINER_REGISTRY_ENDPOINT)

            IMAGE_TAG="${{ github.sha }}"
            publish_astervoids_image "$ACR_NAME" "$ACR_LOGIN_SERVER" "$IMAGE_TAG"
            FULL_IMAGE="$PUBLISHED_IMAGE"

            # Update every region's container app with the new image.
            echo "$REGIONS_JSON" | jq -r '.[].name' | while read -r REGION_NAME; do
              CONTAINER_APP_NAME="${{ env.CONTAINER_APP_PREFIX }}-production-${REGION_NAME}"
              echo "::group::Updating $CONTAINER_APP_NAME ($REGION_NAME)"
              az containerapp update \
                --name "$CONTAINER_APP_NAME" \
                --resource-group "${{ env.PRODUCTION_RG }}" \
                --image "$FULL_IMAGE" \
                --output table
              echo "::endgroup::"
            done

            # Surface static apex + per-region details in the run log.
            STATIC_WEB_APP_NAME=$(deployment_output "$OUTPUTS_JSON" STATIC_WEB_APP_NAME)
            STATIC_WEB_APP_DEFAULT_HOSTNAME=$(deployment_output "$OUTPUTS_JSON" STATIC_WEB_APP_DEFAULT_HOSTNAME)
            echo "Static Web App: ${STATIC_WEB_APP_NAME:-<none>}"
            echo "Static apex default hostname: ${STATIC_WEB_APP_DEFAULT_HOSTNAME:-<none>}"
            echo "Per-region endpoints:"
            deployment_output_json "$OUTPUTS_JSON" REGION_ENDPOINTS | jq .

            # Use the primary region's URI as the canonical deploy URL.
            azd env set WEB_URI "$(deployment_output "$OUTPUTS_JSON" WEB_URI)"
            azd env set CONTAINER_APP_NAME "${{ env.CONTAINER_APP_PREFIX }}-production-$(echo "$REGIONS_JSON" | jq -r '.[0].name')"
            azd env set CONTAINER_APPS_ENVIRONMENT "${{ env.CONTAINER_ENV_NAME }}-$(echo "$REGIONS_JSON" | jq -r '.[0].name')"
            azd env set RESOURCE_GROUP "${{ env.PRODUCTION_RG }}"

            # Build the static apex payload from wwwroot + generated region bootstrap.
            STATIC_REGION_MANIFEST=$(deployment_output_json "$OUTPUTS_JSON" STATIC_APEX_REGION_MANIFEST)
            if [ -z "$STATIC_REGION_MANIFEST" ]; then
              STATIC_REGION_MANIFEST='[]'
            fi
            STATIC_DIR="$GITHUB_WORKSPACE/.tmp/static-apex"
            rm -rf "$STATIC_DIR"
            mkdir -p "$STATIC_DIR"
            cp -R AstervoidsWeb/wwwroot/. "$STATIC_DIR"/
            BOOTSTRAP_JSON=$(jq -c -n --argjson regions "$STATIC_REGION_MANIFEST" \
              '{ regionId: null, displayName: null, regions: $regions }')
            printf 'window.ASTERVOIDS_REGION_BOOTSTRAP = %s;\n' "$BOOTSTRAP_JSON" > "$STATIC_DIR/region-bootstrap.js"
            echo "Prepared static apex payload at $STATIC_DIR"
            echo "static_web_app_name=$STATIC_WEB_APP_NAME" >> $GITHUB_OUTPUT
            echo "static_web_app_default_hostname=$STATIC_WEB_APP_DEFAULT_HOSTNAME" >> $GITHUB_OUTPUT
            echo "static_deploy_dir=$STATIC_DIR" >> $GITHUB_OUTPUT
          else
            # Branch: provision with azd, then deploy with az cli to specific container app
            # First, provision the infrastructure
            azd provision --no-prompt
            AZD_ENV_VALUES=$(azd env get-values)

            # Defensive check: `azd provision --no-prompt` can falsely report
            # "no changes" for branch deploys when a prior branch's truncated
            # 20-char sanitized env name matches this one (azd's deployment
            # state is keyed on env name, not on the actual resources). When
            # that happens, our branch's container app is silently NOT created
            # and the subsequent `az containerapp update` fails with
            # "The containerapp '...' does not exist". Verify the expected app
            # exists in the production resource group; if not, force a direct
            # bicep deployment to create it.
            EXPECTED_APP="${{ env.CONTAINER_APP_PREFIX }}-${SANITIZED_BRANCH}"
            if ! az containerapp show \
                  --name "$EXPECTED_APP" \
                  --resource-group "${{ env.PRODUCTION_RG }}" \
                  &>/dev/null; then
              echo "::warning::Container app '$EXPECTED_APP' missing after azd provision (likely the env-name truncation collision in azd's deployment state). Forcing a direct bicep deployment to create it."

              CD_NAME=$(azd_env_value CUSTOM_DOMAIN_NAME "$AZD_ENV_VALUES")
              CD_SUB=$(azd_env_value CUSTOM_SUBDOMAIN "$AZD_ENV_VALUES")
              DOMAIN_VERIFICATION_ID=$(azd_env_value DOMAIN_VERIFICATION_ID "$AZD_ENV_VALUES")

              az deployment sub create \
                --name "fallback-${SANITIZED_BRANCH}-${{ github.run_id }}" \
                --location westus2 \
                --template-file infra/main.bicep \
                --parameters \
                  environmentName="${SANITIZED_BRANCH}" \
                  location=westus2 \
                  useSharedInfra=true \
                  regions="$REGIONS_JSON" \
                  customDomainName="$CD_NAME" \
                  customSubdomain="$CD_SUB" \
                  certKeyVaultSecretUrl="$CERT_KEY_VAULT_SECRET_URL" \
                  certKeyVaultCertName="$CERT_KEY_VAULT_CERT_NAME" \
                  certReaderIdentityId="$CERT_READER_IDENTITY_ID" \
                  domainVerificationId="$DOMAIN_VERIFICATION_ID"

              # Re-verify after the fallback deployment.
              if ! az containerapp show \
                    --name "$EXPECTED_APP" \
                    --resource-group "${{ env.PRODUCTION_RG }}" \
                    &>/dev/null; then
                echo "::error::Container app '$EXPECTED_APP' still missing after fallback bicep deployment. Aborting."
                exit 1
              fi
            fi

            # Get the container registry name from azd outputs
            ACR_NAME=$(azd_env_value AZURE_CONTAINER_REGISTRY_NAME "$AZD_ENV_VALUES")
            ACR_LOGIN_SERVER=$(azd_env_value AZURE_CONTAINER_REGISTRY_ENDPOINT "$AZD_ENV_VALUES")
            
            # Build and push image using Docker
            IMAGE_TAG="${{ github.sha }}"
            publish_astervoids_image "$ACR_NAME" "$ACR_LOGIN_SERVER" "$IMAGE_TAG"
            FULL_IMAGE="$PUBLISHED_IMAGE"
            
            # Update the specific branch container app with the new image
            CONTAINER_APP_NAME="${{ env.CONTAINER_APP_PREFIX }}-${SANITIZED_BRANCH}"
            echo "Deploying to container app: $CONTAINER_APP_NAME"
            
            az containerapp update \
              --name "$CONTAINER_APP_NAME" \
              --resource-group "${{ env.PRODUCTION_RG }}" \
              --image "$FULL_IMAGE"
          fi
          
          # Get outputs from azd
          AZD_ENV_VALUES=$(azd env get-values)
          if ! DEPLOY_URL=$(require_azd_env_value WEB_URI "$AZD_ENV_VALUES"); then
            echo "::error::Deployment did not produce a WEB_URI."
            exit 1
          fi
          echo "url=$DEPLOY_URL" >> $GITHUB_OUTPUT
          
          # Export values for custom domain step
          echo "CONTAINER_APP_NAME=$(azd_env_value CONTAINER_APP_NAME "$AZD_ENV_VALUES")" >> $GITHUB_ENV
          echo "CONTAINER_APPS_ENVIRONMENT=$(azd_env_value CONTAINER_APPS_ENVIRONMENT "$AZD_ENV_VALUES")" >> $GITHUB_ENV
          echo "RESOURCE_GROUP=$(azd_env_value RESOURCE_GROUP "$AZD_ENV_VALUES")" >> $GITHUB_ENV
          echo "IS_MULTI_REGION=${IS_MULTI_REGION}" >> $GITHUB_ENV

          # Resolve custom domain explicitly for multi-region (azd env outputs
          # from direct `az deployment sub create` don't always carry
          # CUSTOM_DOMAIN). Fall back to azd output for other paths.
          if [ "$IS_MULTI_REGION" = "true" ] && [ -n "$CD_NAME" ] && [ -n "$CD_SUB" ]; then
            CUSTOM_DOMAIN="${CD_SUB}.${CD_NAME}"
            azd env set CUSTOM_DOMAIN "$CUSTOM_DOMAIN"
          else
            CUSTOM_DOMAIN=$(azd_env_value CUSTOM_DOMAIN "$AZD_ENV_VALUES")
          fi
          if [ -n "$CUSTOM_DOMAIN" ]; then
            echo "CUSTOM_DOMAIN=$CUSTOM_DOMAIN" >> $GITHUB_ENV
          fi
        env:
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Get static apex deploy token (multi-region)
        id: swa_publish
        if: steps.vars.outputs.is_production == 'true' && env.IS_MULTI_REGION == 'true' && steps.deploy.outputs.static_web_app_name != ''
        run: |
          TOKEN=$(az staticwebapp secrets list \
            --name "${{ steps.deploy.outputs.static_web_app_name }}" \
            --resource-group "${{ env.PRODUCTION_RG }}" \
            --query "properties.apiKey" -o tsv)
          if [ -z "$TOKEN" ]; then
            echo "::error::Failed to fetch Static Web App deployment token via az staticwebapp secrets list."
            exit 1
          fi
          echo "::add-mask::$TOKEN"
          echo "api_token=$TOKEN" >> $GITHUB_OUTPUT

      - name: Publish static apex payload (multi-region)
        if: steps.vars.outputs.is_production == 'true' && env.IS_MULTI_REGION == 'true' && steps.deploy.outputs.static_web_app_name != ''
        uses: Azure/static-web-apps-deploy@v1
        with:
          action: upload
          app_location: ${{ steps.deploy.outputs.static_deploy_dir }}
          skip_app_build: true
          azure_static_web_apps_api_token: ${{ steps.swa_publish.outputs.api_token }}

      - name: Bind static apex custom hostname (multi-region)
        if: steps.vars.outputs.is_production == 'true' && env.IS_MULTI_REGION == 'true' && env.CUSTOM_DOMAIN != '' && steps.deploy.outputs.static_web_app_name != ''
        run: |
          SWA_NAME="${{ steps.deploy.outputs.static_web_app_name }}"
          echo "Binding static apex hostname '$CUSTOM_DOMAIN' on '$SWA_NAME'."
          MAX_RETRIES=20
          for attempt in $(seq 1 $MAX_RETRIES); do
            status=$(az staticwebapp hostname show \
              --name "$SWA_NAME" \
              --resource-group "${{ env.PRODUCTION_RG }}" \
              --hostname "$CUSTOM_DOMAIN" \
              --query "status" -o tsv 2>/dev/null || echo "")
            if [ "$status" = "Ready" ]; then
              echo "✅ Static apex hostname is Ready"
              exit 0
            fi

            echo "Attempt $attempt/$MAX_RETRIES: ensure hostname binding exists (current status: ${status:-missing})"
            az staticwebapp hostname set \
              --name "$SWA_NAME" \
              --resource-group "${{ env.PRODUCTION_RG }}" \
              --hostname "$CUSTOM_DOMAIN" \
              --no-wait \
              --output none || true

            sleep 20
          done

          final_status=$(az staticwebapp hostname show \
            --name "$SWA_NAME" \
            --resource-group "${{ env.PRODUCTION_RG }}" \
            --hostname "$CUSTOM_DOMAIN" \
            --query "status" -o tsv 2>/dev/null || echo "missing")
          echo "::error::Static apex hostname did not reach Ready. Final status: $final_status"
          exit 1

      - name: Configure Custom Domain (if enabled)
        # Runs ONLY for single-region production WITHOUT BYO cert vars.
        # All other cases are handled in-band by bicep + the bootstrap step:
        #   - Single-region production WITH BYO    → bicep binds via SNI.
        #   - Multi-region production              → bicep + static-apex publish
        #                                            steps above (no ACA managed-cert flow).
        #   - Branches WITH BYO                    → bootstrap step (above)
        #                                            ensures cert + identity on
        #                                            the shared CAE; bicep then
        #                                            binds the branch hostname
        #                                            via SNI. ~5 min saved vs
        #                                            the legacy managed-cert flow.
        #   - Branches WITHOUT BYO                 → this step runs, the
        #                                            legacy DigiCert flow
        #                                            creates + binds a managed
        #                                            cert per branch.
        if: env.CUSTOM_DOMAIN != '' && env.IS_MULTI_REGION != 'true' && env.CERT_KEY_VAULT_SECRET_URL == ''
        continue-on-error: true
        run: bash .github/scripts/configure-managed-domain.sh
        env:
          AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deployment Summary
        run: |
          source .github/scripts/deployment-helpers.sh
          echo "### 🚀 Deployment Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.vars.outputs.is_production }}" = "true" ]; then
            echo "**Environment:** production" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Environment:** preview (${{ steps.vars.outputs.branch_name }})" >> $GITHUB_STEP_SUMMARY
          fi
          echo "**Application URL:** ${{ steps.deploy.outputs.url }}" >> $GITHUB_STEP_SUMMARY
          if [ -n "$CUSTOM_DOMAIN" ]; then
            # SECURITY: job summaries are NOT secret-masked (only logs are), and
            # this repo is public. The custom hostname embeds the secret
            # CUSTOM_SUBDOMAIN/CUSTOM_DOMAIN_NAME (apex for production, suffixed for
            # branches), so printing it here would publicly correlate this repo to
            # the live deployment. Never emit it — resolve it privately instead with
            # .github/scripts/branch-url.sh.
            echo "**Custom domain:** configured (private — run \`.github/scripts/branch-url.sh\` locally to resolve)" >> $GITHUB_STEP_SUMMARY
          fi

          # Multi-region: list per-region container apps + static apex host info
          # so reviewers can spot-check each endpoint from the summary.
          if [ "$IS_MULTI_REGION" = "true" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Multi-region rollout:** $(echo "$REGIONS_JSON" | jq 'length') regions" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Region | Container App | FQDN |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|---------------|------|" >> $GITHUB_STEP_SUMMARY
            # Re-fetch outputs JSON for the multi-region deployment.
            SUMMARY_OUTPUTS=$(az deployment sub show \
              --name "production-multi-${{ github.run_id }}" \
              --query 'properties.outputs' -o json)
            deployment_output_json "$SUMMARY_OUTPUTS" REGION_ENDPOINTS \
              | jq -r '.[] | "| \(.displayName) (\(.id)) | \(.containerAppName) | https://\(.fqdn) |"' \
              >> $GITHUB_STEP_SUMMARY
            SWA_FQDN=$(deployment_output "$SUMMARY_OUTPUTS" STATIC_WEB_APP_DEFAULT_HOSTNAME)
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Static apex default host:** \`$SWA_FQDN\` (apex CNAME target)" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "View your deployment in the [Azure Portal](https://portal.azure.com)" >> $GITHUB_STEP_SUMMARY
